Crear carpeta "Models"

Entrar en entorno virtual:

py -m venv venv
venv\Scripts\activate

Ejecutar:

pip install -r requirements.txt

Despues ejecutar:

py train.py

Para generar los modelos, despues ejecutar:

py chatbot.py